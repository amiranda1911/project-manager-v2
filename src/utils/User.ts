export interface User {
    id: number,
    firstName: string,
    lastName: string,
    username: string,
    createdAt: number, //timestamp
    email: string,
    password: string,
    jobPosition: string, 
}